---
type: llm
focus: trace
weight: 1
---
This item is marked `[minor]` because the fix is small and non-urgent - that is not a
license to write the line vaguely. Per the allowlist principle (the skill's own Ground
rules and `references/categories.md`), a category still gets a specific, named action
regardless of priority or whether it dispatches this run.

Fail this if the logged line settles for something generic like "some UX idea" or "minor
visual thing to look at" instead of naming the actual fix (swap the fuel-card icon to the
standard palette blue). "Minor" describes urgency, not the required specificity of the
line.
