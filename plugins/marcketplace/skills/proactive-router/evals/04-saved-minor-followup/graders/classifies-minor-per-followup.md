---
type: llm
focus: trace
weight: 1
---
The fuel-card icon colour item (permalink containing `DEXAMPLEICONUX1`) was surfaced only via
`is:saved` - no emoji reaction - and the colleague's root message alone reads as inert
("worth aligning at some point?"). The user's own follow-up reply confirms it should
change (swap the icon to the standard palette blue, no rush).

A good surface line:
- Is still logged despite being saved-only (not discarded as out of scope).
- Uses the 🎫 emoji, marked minor/park (e.g. "[minor]"), matching the "small, non-urgent
  fix" category - not urgent, but still a real ticket per the follow-up.
- Reflects the actual fix confirmed (icon colour swap), not just "some UX idea".

Fail this if the item is missing entirely, if it's logged as FYI-only instead of a ticket
(missing the follow-up confirmation), or if it's treated as a full-priority ticket rather
than minor/non-urgent.
