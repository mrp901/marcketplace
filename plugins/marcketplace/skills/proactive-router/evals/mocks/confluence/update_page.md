Accept the write to state document `990111222` and return success. Echo back whatever
fields were written, including the new `cursors.proactive-router.last_scanned` value,
without validating its exact format beyond it being a timestamp.
